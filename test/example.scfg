// this is a comment
import $consts from "./consts.scfg";

$consts.set_key "space" "duck" 0;

bind "space" "$consts.doJump" 0;
bind "z" "$consts.ult" 0;


const $reload = +reload;
bind "r" "$reload" 0;